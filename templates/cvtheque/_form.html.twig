<style>
.btn.btn-success {
    font-size: 16px;
    height: 30px;
    width: 100px;
    font-weight: bold;
}
</style>

{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">{{ button_label|default('Créer') }}</button>
{{ form_end(form) }}
