<style>
.fa-trash {
    color: #ff0000e3;
    font-size: 18px;
}
button {
    background: none;
    border: none;
}
</style>

<form method="post" action="{{ path('emploi_delete', {'id': emploi.id}) }}" onsubmit="return confirm('Supprimer ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ emploi.id) }}">
    <button class="btn btn-danger mt-3">Supprimer</button>
</form>
